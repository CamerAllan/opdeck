import { CSSProperties } from "react";

export const selectCont = {
    display: "flex",
    flex: "1 0",
    flexFlow: "column wrap"
} as CSSProperties;

export const selectMenu = {
    display: "flex",
    flexFlow: "row"
} as CSSProperties;

export const selectable = {
    flex: "1 0",
    height: "25px"
} as CSSProperties;

export const limitCont = {
    display: "flex",
    flexFlow: "row"
} as CSSProperties

export const limit = {
    flex: "1 0 auto"
} as CSSProperties